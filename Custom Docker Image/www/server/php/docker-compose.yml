version: '3'

services:
  custom_php:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "9000:9000"
    volumes:
      - ./www/website/path:/usr/share/nginx/html # 网站根目录
    networks:
      - my_network

networks:
  my_network:
    driver: bridge
